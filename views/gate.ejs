<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GATE FRAMEWORK - Pink Concept</title>
    <style>
        /* ตั้งค่า body สีพื้นหลังชมพูอ่อน และฟอนต์สบายตา */
        body {
            background: linear-gradient(135deg, #ffe6f0, #ffccd9);
            font-family: 'Arial', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        /* สำหรับหัวข้อหลัก */
        .gate-intro {
            color: #ff3399; /* ชมพูเข้ม */
            font-size: 3rem;
            font-weight: bold;
            text-shadow: 2px 2px 10px rgba(255, 51, 153, 0.5); /* เงาชมพูนุ่ม */
            animation: bounce 2s infinite;
            text-align: center;
        }

        /* Animation เด้งๆ */
        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        /* เพิ่มปุ่มชมพูน่ารัก */
        .pink-button {
            margin-top: 30px;
            padding: 12px 24px;
            font-size: 1.2rem;
            color: white;
            background-color: #ff66aa;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            transition: transform 0.2s, box-shadow 0.2s;
        }

        .pink-button:hover {
            transform: scale(1.1);
            box-shadow: 0 5px 15px rgba(255, 102, 170, 0.6);
        }

        /* จัด container ให้รวมหัวข้อและปุ่ม */
        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

    </style>
</head>
<body>
    <div class="container">
        <h1 class="gate-intro">Hello Gate 🎉</h1>
        <button class="pink-button">Click Me 💖</button>
    </div>
</body>
</html>
